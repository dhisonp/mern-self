import React from "react";
import { Text } from "@chakra-ui/react";

const ToDo = (props) => {
  return (
    <Flex bg="pink" minH={"100vh"} align={"center"} justify={"center"}>
        <Text>This would be the ToDo app<Text/>    
    </Flex>
  );
};

export default ToDo;
